# Webアプリケーションアーキテクチャ案

## 概要
このドキュメントでは、ポモドーロタイマーWebアプリケーションのアーキテクチャ案をまとめます。このアプリケーションは、Flaskをバックエンドフレームワークとして使用し、HTML/CSS/JavaScriptをフロントエンドに使用します。

---

## アーキテクチャ概要
1. **バックエンド (Flask)**
   - タイマーのロジックや状態管理を担当。
   - REST APIを提供して、フロントエンドと通信。
   - 必要に応じて、セッションやデータベースを使用してタイマーの履歴を保存。

2. **フロントエンド (HTML/CSS/JavaScript)**
   - ユーザーインターフェースを提供。
   - JavaScriptでタイマーのカウントダウンを実装。
   - 必要に応じて、バックエンドと非同期通信 (AJAX/Fetch API) を行う。

3. **静的ファイル**
   - CSS: UIのスタイリング。
   - JavaScript: タイマーの動作やユーザー操作の処理。
   - 画像: UIモックにあるようなアイコンや背景画像。

4. **テンプレートエンジン**
   - Flaskの`Jinja2`テンプレートエンジンを使用して、動的なHTMLを生成。

---

## ディレクトリ構成
以下のようなディレクトリ構成を推奨します：

```
2025-Github-Copilot-Workshop-Python/
│
├── static/
│   ├── css/
│   │   └── style.css  # アプリのスタイル
│   ├── js/
│   │   └── timer.js   # タイマーのフロントエンドロジック
│   └── images/
│       └── pomodoro.png  # UIモック画像
│
├── templates/
│   └── index.html  # メインのHTMLテンプレート
│
├── app.py  # Flaskアプリのエントリーポイント
├── requirements.txt  # 必要なPythonパッケージ
└── README.md  # プロジェクトの説明
```

---

## 機能設計
### 1. **タイマー機能**
   - ユーザーがポモドーロタイマーを開始、停止、リセットできる。
   - タイマーのデフォルト設定（25分作業、5分休憩）を提供。
   - 必要に応じて、カスタム時間を設定可能。

### 2. **通知機能**
   - タイマー終了時に音や画面上の通知を表示。

### 3. **履歴管理 (オプション)**
   - ユーザーが過去のポモドーロセッションを確認できる。

---

## 技術スタック
1. **バックエンド**
   - Flask
   - Python標準ライブラリ (datetimeなど)
   - 必要に応じてSQLAlchemyやSQLiteを使用

2. **フロントエンド**
   - HTML5
   - CSS3 (FlexboxやGridを活用)
   - JavaScript (Vanilla JSまたは軽量ライブラリ)

3. **その他**
   - 必要に応じて、BootstrapやTailwind CSSなどのCSSフレームワークを使用。

---

## ユニットテストのしやすさを考慮した改善点

### 1. **バックエンドのモジュール化**
- Flaskアプリケーションを以下のように分割：
  - `routes/`: ルートハンドラを管理。
  - `services/`: ビジネスロジックを管理。
  - `tests/`: ユニットテストを管理。

**ディレクトリ構成の例:**
```
2025-Github-Copilot-Workshop-Python/
├── app.py  # アプリのエントリーポイント
├── routes/
│   └── timer_routes.py  # タイマー関連のルート
├── services/
│   └── timer_service.py  # タイマーのビジネスロジック
├── tests/
│   ├── test_timer_service.py  # ビジネスロジックのテスト
│   └── test_timer_routes.py  # ルートのテスト
```

### 2. **依存性の注入 (Dependency Injection)**
- サービス層を分離し、ルートから呼び出すことでテスト可能な構造に。

### 3. **テスト用の設定ファイル**
- Flaskの設定ファイルを分離し、テスト用の設定を追加。

### 4. **ユニットテストのカバレッジ向上**
- サービス層とルートのテストを追加。
- 異常系の動作を確認するテストを実装。

### 5. **モックとスタブの活用**
- `unittest.mock`を使用して、外部依存をモック化。

---

## 次のステップ
1. **ディレクトリ構成の作成**
   - 上記の構成に基づいて、必要なフォルダとファイルを作成します。

2. **Flaskアプリの初期設定**
   - `app.py`にFlaskアプリの基本構造を記述します。

3. **フロントエンドのUI作成**
   - `index.html`と`style.css`を作成し、UIモックに基づいてデザインを実装します。

4. **タイマーのロジック実装**
   - JavaScriptでタイマーのカウントダウン機能を実装します。

5. **バックエンドとの連携**
   - 必要に応じて、タイマーの状態をバックエンドで管理し、フロントエンドと通信します。

---

このアーキテクチャ案を基に、プロジェクトを進めていきましょう。必要に応じて、各ステップで詳細なサポートを提供します！
